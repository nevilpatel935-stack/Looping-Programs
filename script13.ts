const P= 50000;
const N=5;
const R=0.07;

const intrest=P*N*R;
const netAmount=P+intrest;

console.log(`p is:${P}`);
console.log(`n is:${N}`);
console.log(`r is:${R}`);
console.log(`intrest is:${intrest}`);
console.log(`netAmount is:${netAmount}`);